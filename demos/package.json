{
  "name": "spatial-index-demos",
  "version": "1.0.0",
  "description": "spatial index web demos",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "spatial-index": "workspace:*"
  },
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "start": "webpack-dev-server --config webpack.config.js",
    "deploy": "gh-pages -d dist"
  }
}